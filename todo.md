## Loop coordinate labels without time breaks

* Cyclical labels should also be used for specific break points (not just time breaks)
* The labels should show the number of months within the current loop (e.g. M0, M1, M2, ... M14), up to the maximum length between break points.